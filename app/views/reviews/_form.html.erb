<%= simple_form_for([ nihonshu, review ], remote: true) do |form| %>
  <!-- <%= form.input :title, as: :text %> -->
  <%= form.input :description, as: :text %>
  <%= form.input :rating, as: :select, collection: 1..5 %>
  <%= form.button :submit %>
<% end %>
